# Server GPU STT (faster-whisper + CUDA)
# Install on STT server: pip install -r requirements-server.txt
faster-whisper>=1.0.0
# CUDA 12 runtime DLLs for ctranslate2 (cublas64_12.dll etc.)
nvidia-cublas-cu12
nvidia-cudnn-cu12
nvidia-cuda-runtime-cu12
